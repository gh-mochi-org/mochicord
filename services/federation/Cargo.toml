[package]
name = "mochicord-federation"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8.8"
nostr-sdk = "0.44.1"
tokio = { version = "1.50.0", features = ["full"] }
tonic = { version = "0.14.5", features = ["transport", "tls-ring", "gzip"] }
